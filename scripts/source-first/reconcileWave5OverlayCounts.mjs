import fs from 'node:fs'; import path from 'node:path'; import crypto from 'node:crypto'
const root=process.cwd(); const file=path.join(root,'public/data-beta/final-catalogue/manifest.json'); const manifest=JSON.parse(fs.readFileSync(file,'utf8')); manifest.wave5_overlay.workflow_target_count=74; manifest.wave5_overlay.activated_count=74; manifest.wave5_overlay.workflow_evidence_pack_count=74; manifest.fingerprints.app_manifest=crypto.createHash('sha256').update(JSON.stringify(manifest)).digest('hex'); fs.writeFileSync(file,`${JSON.stringify(manifest,null,2)}\n`); console.log(JSON.stringify(manifest.wave5_overlay,null,2))
