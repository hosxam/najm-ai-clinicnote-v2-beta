import fs from 'node:fs'
import path from 'node:path'
const root = process.cwd(); const dir = path.join(root, 'public/data-beta/final-catalogue'); const catalogPath = path.join(dir, 'catalog.json'); const manifest = JSON.parse(fs.readFileSync(path.join(dir, 'manifest.json'), 'utf8')); const catalog = JSON.parse(fs.readFileSync(catalogPath, 'utf8')); catalog.workflow_count = 1500; catalog.usable_workflow_count = manifest.counts.active_workflows; catalog.inactive_workflow_count = manifest.counts.inactive_workflows; catalog.user_facing_item_count = manifest.counts.clinician_facing_items; catalog.internal_evidence_record_count = manifest.counts.internal_evidence_records; fs.writeFileSync(catalogPath, `${JSON.stringify(catalog, null, 2)}\n`); console.log(JSON.stringify({ workflow_count: catalog.workflow_count, usable: catalog.usable_workflow_count, inactive: catalog.inactive_workflow_count }, null, 2))
