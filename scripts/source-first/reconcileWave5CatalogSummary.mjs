import fs from 'node:fs'; import path from 'node:path'
const root=process.cwd(); const dir=path.join(root,'public/data-beta/final-catalogue'); const read=f=>JSON.parse(fs.readFileSync(f,'utf8')); const write=(f,v)=>fs.writeFileSync(f,`${JSON.stringify(v,null,2)}\n`); const manifest=read(path.join(dir,'manifest.json')); const catalog=read(path.join(dir,'catalog.json')); catalog.workflow_count=manifest.counts.original_workflows; catalog.usable_workflow_count=manifest.counts.active_workflows; catalog.inactive_workflow_count=manifest.counts.inactive_workflows; catalog.user_facing_item_count=manifest.counts.clinician_facing_items; catalog.internal_evidence_record_count=manifest.counts.internal_evidence_records; write(path.join(dir,'catalog.json'),catalog); console.log(JSON.stringify({active:catalog.usable_workflow_count,inactive:catalog.inactive_workflow_count},null,2))
